import { StyleSheet } from "react-native";

export default StyleSheet.create({
    LogoContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'center',
        position: 'absolute',
        top: 20
    },
    logoText: {
        fontWeight: "900",
        fontSize:20
    }
})